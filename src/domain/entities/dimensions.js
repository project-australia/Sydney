export class Dimensions {
  height
  length
  width
  weight

  constructor (height, length, width, weight) {
    this.height = height
    this.length = length
    this.width = width
    this.weight = weight
  }
}
